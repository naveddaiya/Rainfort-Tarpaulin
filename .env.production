# Cloudinary — public upload config (safe to commit, visible in every network request)
VITE_CLOUDINARY_CLOUD_NAME=dfykn2rug
VITE_CLOUDINARY_UPLOAD_PRESET=ecommerce_products

# Razorpay publishable key (safe to commit — this is a public key, not the secret)
VITE_RAZORPAY_KEY_ID=rzp_test_SEXgStRsRU2IQo
